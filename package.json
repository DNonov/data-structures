{
  "name": "dstructures",
  "version": "1.1.1",
  "description": "Collection of data structures",
  "main": "index.js",
  "scripts": {
    "test": "jest --watchAll",
    "test_ci": "jest",
    "cover": "istanbul cover npm run test_ci",
    "coveralls": "istanbul cover npm run test_ci && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage",
    "doc-build": "jsdoc -c doc-config.json"
  },
  "author": "DNonov",
  "license": "MIT",
  "devDependencies": {
    "@jeremyckahn/minami": "^1.3.1",
    "@types/jest": "^24.0.13",
    "coveralls": "^3.0.0",
    "istanbul": "^0.4.5",
    "jest": "^24.8.0",
    "jsdoc": "^3.5.5"
  },
  "dependencies": {}
}
